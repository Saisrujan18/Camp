Home page responsive

collab page
    - new collab modal

club page
profile page


backend - routers, server setup, models.


BUG fixes for mongoDB:
    -make sure URL is proper -> name, password and the database name
    -timeout 10000ms error -> make sure the database is connected before doing any query (read docs);

